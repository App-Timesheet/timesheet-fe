benim bir tespitim var


import axios from "axios";

const apiClient = axios.create({
  baseURL: process.env.REACT_APP_API_URL || "https://timesheet-be-latest.onrender.com/",
  timeout: 60000, 
  headers: {
    "Content-Type": "multipart/form-data", 
  },
});

apiClient.interceptors.request.use(
  (config) => {
    const token = localStorage.getItem("token");
    if (token) {
      config.headers["Authorization"] = `Bearer ${token}`;
    }
    return config;
  },
  (error) => {
    return Promise.reject(error);
  }
);

apiClient.interceptors.response.use(
  (response) => response,
  (error) => {
    return Promise.reject(error);
  }
);

export default apiClient;

Benim interceptor dosyam böyleyken proje oluşturuluyor 201 geliyor. Ama giriş ve kullanıcı kayıt kısımları çalışmıyor.























import axios from "axios";

const apiClient = axios.create({
  baseURL: process.env.REACT_APP_API_URL || "https://timesheet-be-latest.onrender.com/",
  timeout: 60000,
  headers: {
    "Content-Type": "application/json", // multipart yerine json kullanın
  },
});


apiClient.interceptors.request.use(
  (config) => {
    const token = localStorage.getItem("token");
    if (token) {
      config.headers["Authorization"] = `Bearer ${token}`;
    }
    return config;
  },
  (error) => {
    return Promise.reject(error);
  }
);

apiClient.interceptors.response.use(
  (response) => response,
  (error) => {
    return Promise.reject(error);
  }
);

export default apiClient;




Böyleyken de giriş ve kayıt çalışıyor ama proje ekleme çalışmıyor.























